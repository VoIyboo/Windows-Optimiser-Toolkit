<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <SolidColorBrush x:Key="QOTScrollTrackBrush" Color="#1E293B" />
    <SolidColorBrush x:Key="QOTScrollTrackBorderBrush" Color="#334155" />
    <SolidColorBrush x:Key="QOTScrollButtonHoverBrush" Color="#111827" />
    <SolidColorBrush x:Key="QOTScrollThumbBrush" Color="#3B82F6" />
    <SolidColorBrush x:Key="QOTScrollThumbHoverBrush" Color="#60A5FA" />
    <SolidColorBrush x:Key="QOTScrollThumbPressedBrush" Color="#93C5FD" />
    <SolidColorBrush x:Key="QOTScrollGlyphBrush" Color="#BFDBFE" />

    <Style x:Key="QOTScrollThumbStyle" TargetType="{x:Type Thumb}">
        <Setter Property="Background" Value="{StaticResource QOTScrollThumbBrush}" />
        <Setter Property="BorderBrush" Value="#38BDF8" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="MinHeight" Value="40" />
        <Setter Property="MinWidth" Value="40" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Thumb}">
                    <Border x:Name="ThumbRoot"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="5"
                            SnapsToDevicePixels="True">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="12"
                                              Direction="270"
                                              Opacity="0.65"
                                              ShadowDepth="0"
                                              Color="#38BDF8" />
                        </Border.Effect>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ThumbRoot" Property="Background" Value="{StaticResource QOTScrollThumbHoverBrush}" />
                            <Setter TargetName="ThumbRoot" Property="BorderBrush" Value="#BAE6FD" />
                        </Trigger>
                        <Trigger Property="IsDragging" Value="True">
                            <Setter TargetName="ThumbRoot" Property="Background" Value="{StaticResource QOTScrollThumbPressedBrush}" />
                            <Setter TargetName="ThumbRoot" Property="BorderBrush" Value="#E0F2FE" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="ThumbRoot" Property="Opacity" Value="0.45" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="QOTScrollRepeatButtonStyle" TargetType="{x:Type RepeatButton}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type RepeatButton}">
                    <Border x:Name="ButtonRoot"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4"
                            SnapsToDevicePixels="True">
                        <Path x:Name="Glyph"
                              Width="7"
                              Height="7"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center"
                              Fill="{StaticResource QOTScrollGlyphBrush}"
                              Stretch="Uniform"
                              Visibility="Collapsed" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ButtonRoot" Property="Background" Value="{StaticResource QOTScrollButtonHoverBrush}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="ButtonRoot" Property="Background" Value="#16284A" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="ButtonRoot" Property="Opacity" Value="0.35" />
                        </Trigger>
                        <Trigger Property="Tag" Value="LineUp">
                            <Setter TargetName="Glyph" Property="Data" Value="M 3.5 0 L 7 5.5 L 0 5.5 Z" />
                            <Setter TargetName="Glyph" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="Tag" Value="LineDown">
                            <Setter TargetName="Glyph" Property="Data" Value="M 0 1.5 L 7 1.5 L 3.5 7 Z" />
                            <Setter TargetName="Glyph" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="Tag" Value="LineLeft">
                            <Setter TargetName="Glyph" Property="Data" Value="M 0 3.5 L 5.5 0 L 5.5 7 Z" />
                            <Setter TargetName="Glyph" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="Tag" Value="LineRight">
                            <Setter TargetName="Glyph" Property="Data" Value="M 1.5 0 L 7 3.5 L 1.5 7 Z" />
                            <Setter TargetName="Glyph" Property="Visibility" Value="Visible" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <ControlTemplate x:Key="QOTVerticalScrollBarTemplate" TargetType="{x:Type ScrollBar}">
        <Grid Width="{TemplateBinding Width}" SnapsToDevicePixels="True">
            <Border
                    Background="{StaticResource QOTScrollTrackBrush}"
                    BorderBrush="{StaticResource QOTScrollTrackBorderBrush}"
                    BorderThickness="1"
                    CornerRadius="5">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="6"
                                      Direction="270"
                                      Opacity="0.18"
                                      ShadowDepth="0"
                                      Color="#0F172A" />
                </Border.Effect>
            </Border>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Track x:Name="PART_Track"
                       Grid.Row="0"
                       IsDirectionReversed="True">
                    <Track.DecreaseRepeatButton>
                        <RepeatButton Style="{StaticResource QOTScrollRepeatButtonStyle}"
                                      Command="ScrollBar.PageUpCommand" />
                    </Track.DecreaseRepeatButton>
                    <Track.Thumb>
                        <Thumb Style="{StaticResource QOTScrollThumbStyle}" />
                    </Track.Thumb>
                    <Track.IncreaseRepeatButton>
                        <RepeatButton Style="{StaticResource QOTScrollRepeatButtonStyle}"
                                      Command="ScrollBar.PageDownCommand" />
                    </Track.IncreaseRepeatButton>
                </Track>
            </Grid>
        </Grid>
    </ControlTemplate>

    <ControlTemplate x:Key="QOTHorizontalScrollBarTemplate" TargetType="{x:Type ScrollBar}">
        <Grid Height="{TemplateBinding Height}" SnapsToDevicePixels="True">
            <Border
                    Background="{StaticResource QOTScrollTrackBrush}"
                    BorderBrush="{StaticResource QOTScrollTrackBorderBrush}"
                    BorderThickness="1"
                    CornerRadius="5">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="6"
                                      Direction="270"
                                      Opacity="0.18"
                                      ShadowDepth="0"
                                      Color="#0F172A" />
                </Border.Effect>
            </Border>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Track x:Name="PART_Track"
                       Grid.Column="0">
                    <Track.DecreaseRepeatButton>
                        <RepeatButton Style="{StaticResource QOTScrollRepeatButtonStyle}"
                                      Command="ScrollBar.PageLeftCommand" />
                    </Track.DecreaseRepeatButton>
                    <Track.Thumb>
                        <Thumb Style="{StaticResource QOTScrollThumbStyle}" />
                    </Track.Thumb>
                    <Track.IncreaseRepeatButton>
                        <RepeatButton Style="{StaticResource QOTScrollRepeatButtonStyle}"
                                      Command="ScrollBar.PageRightCommand" />
                    </Track.IncreaseRepeatButton>
                </Track>
            </Grid>
        </Grid>
    </ControlTemplate>

    <Style TargetType="{x:Type ScrollViewer}">
        <Setter Property="Background" Value="#111827" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>

    <Style TargetType="{x:Type ScrollBar}">
        <Setter Property="Background" Value="{StaticResource QOTScrollTrackBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource QOTScrollTrackBorderBrush}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Width" Value="14" />
        <Setter Property="Height" Value="14" />
        <Setter Property="MinWidth" Value="14" />
        <Setter Property="MinHeight" Value="14" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Style.Triggers>
            <Trigger Property="Orientation" Value="Vertical">
                <Setter Property="Width" Value="14" />
                <Setter Property="MinWidth" Value="14" />
                <Setter Property="Template" Value="{StaticResource QOTVerticalScrollBarTemplate}" />
            </Trigger>
            <Trigger Property="Orientation" Value="Horizontal">
                <Setter Property="Height" Value="14" />
                <Setter Property="MinHeight" Value="14" />
                <Setter Property="Template" Value="{StaticResource QOTHorizontalScrollBarTemplate}" />
            </Trigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>
