<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Help"
        Width="720"
        Height="560"
        WindowStartupLocation="CenterOwner"
        Background="#0F172A">

    <Window.Resources>
        <SolidColorBrush x:Key="AccentBrush" Color="#2563EB" />
        <SolidColorBrush x:Key="CardBrush" Color="#020617" />
        <SolidColorBrush x:Key="BorderBrush" Color="#1F2937" />
        <SolidColorBrush x:Key="TextSubtle" Color="#9CA3AF" />

        <Style x:Key="SectionTitle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontSize" Value="17" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>

        <Style x:Key="SectionBody" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#E5E7EB" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="TextWrapping" Value="Wrap" />
            <Setter Property="LineHeight" Value="20" />
        </Style>
    </Window.Resources>

    <Grid>
        <ScrollViewer
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      CanContentScroll="False"
                      IsDeferredScrollingEnabled="False"
                      PanningMode="VerticalOnly">
            <StackPanel Margin="16">
                <Border Background="#111827"
                        BorderBrush="#1E3A8A"
                        BorderThickness="1"
                        CornerRadius="10"
                        Padding="14"
                        Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="Quick start in 60 seconds"
                                   Foreground="#BFDBFE"
                                   FontSize="14"
                                   FontWeight="SemiBold" />
                        <TextBlock Text="1. Pick your actions and press Play once. 2. Review Logs for worked/skipped/failed outcomes. 3. Use Tickets for email work (notes, replies, status, assignment). 4. Use Settings for mailbox sync and shared paths."
                                   Foreground="#E5E7EB"
                                   FontSize="12.5"
                                   LineHeight="18"
                                   Margin="0,7,0,0" />
                    </StackPanel>
                </Border>

                <ItemsControl x:Name="HelpSections">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{StaticResource CardBrush}"
                                    BorderBrush="{StaticResource BorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="10"
                                    Padding="0"
                                    Margin="0,0,0,12">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="5" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>

                                    <Border Grid.Column="0"
                                            Background="#1D4ED8"
                                            CornerRadius="10,0,0,10" />

                                    <StackPanel Grid.Column="1" Margin="14,12">
                                        <TextBlock Text="{Binding Title}" Style="{StaticResource SectionTitle}" />
                                        <TextBlock Text="{Binding Body}"
                                                   Style="{StaticResource SectionBody}"
                                                   Margin="0,8,0,0" />
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
